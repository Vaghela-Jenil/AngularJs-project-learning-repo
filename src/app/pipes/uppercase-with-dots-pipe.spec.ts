import { UppercaseWithDotsPipe } from './uppercase-with-dots-pipe';

describe('UppercaseWithDotsPipe', () => {
  it('create an instance', () => {
    const pipe = new UppercaseWithDotsPipe();
    expect(pipe).toBeTruthy();
  });
});
